const Calculate_Button = ({label}) => {
  return (
    <button
      className="bg-slate-200 h-12 w-24 rounded-full border-solid text-center text-black text-2xl"
    >
      {label}
    </button>
  );
};

export default Calculate_Button;
